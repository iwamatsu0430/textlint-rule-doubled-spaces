{
  "version": "1.0.1",
  "keywords": [
    "textlintrule"
  ],
  "main": "lib/index.js",
  "files": [
    "lib/",
    "src/"
  ],
  "scripts": {
    "test": "textlint-scripts test",
    "build": "textlint-scripts build",
    "prepublish": "npm run --if-present build"
  },
  "devDependencies": {
    "prettier": "^2.6.2",
    "textlint-scripts": "^13.0.5",
    "textlint-tester": "^13.1.3"
  },
  "name": "textlint-rule-doubled-spaces",
  "description": "textlint for check doubled spaces in sentence.",
  "directories": {
    "test": "test"
  },
  "author": "iwamatsu0430",
  "license": "MIT",
  "dependencies": {
    "@textlint/regexp-string-matcher": "^2.0.1"
  }
}
